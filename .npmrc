# Reproducible, verifiable installs:
# - save-exact: new deps are recorded at an exact version (no ^ drift)
# - engine-strict: refuse to install on a Node/npm outside the pinned range
save-exact=true
engine-strict=true
