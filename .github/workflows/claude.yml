# Interactive Claude responder: mention `@claude` in an issue, PR, or review comment and
# Claude replies (analysis/answers). Read-only — it does not modify the repo; code changes
# go through the label-triggered `claude-issue-implement.yml` loop instead.
#
# On this PUBLIC repo the action's built-in access control only lets users with write
# access (i.e. the maintainer) trigger it. Auth: CLAUDE_CODE_OAUTH_TOKEN (subscription).
name: Claude Code

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  issues:
    types: [opened, assigned]
  pull_request_review:
    types: [submitted]

# Deny by default at the top level, so a job added later inherits NOTHING rather than the
# repository default. The job below then grants only what it reads. (Scorecard
# TokenPermissions: a workflow with no top-level block scores 0 even when every job it
# currently has is already least-privilege — the gap is what the NEXT job would get.)
permissions: {}

jobs:
  claude:
    if: |
      (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review' && contains(github.event.review.body, '@claude')) ||
      (github.event_name == 'issues' && (contains(github.event.issue.body, '@claude') || contains(github.event.issue.title, '@claude')))
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      pull-requests: read
      issues: read
      id-token: write
      actions: read # Required for Claude to read CI results on PRs
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 1

      - name: Run Claude Code
        id: claude
        uses: anthropics/claude-code-action@9d7150bc8a3dae8149739a88019d192b579ad90c # v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          # Let Claude read CI results on PRs.
          additional_permissions: |
            actions: read
